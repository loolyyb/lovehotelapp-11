
import { fetchInitialMessages } from './initialMessagesFetcher';
import { fetchMoreMessages } from './moreMessagesFetcher';

/**
 * Message fetching functions
 */
export const MessagesFetcher = {
  fetchInitialMessages,
  fetchMoreMessages
};
